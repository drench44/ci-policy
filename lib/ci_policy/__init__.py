"""ci-policy: shared PR policy and main-branch watch for drench44 repos.

Standard library only, so the composite actions run on any runner that has
python3 (GitHub-hosted ubuntu-latest and the self-hosted OMEN runners both do).
"""

__version__ = "1.0.0"
